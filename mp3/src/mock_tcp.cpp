// #include "mock_tcp.h"

// tcp_client_intf *get_mock_tcp_client(string hostname, bool show_packets) {
//     return new mock_tcp_client(factory, hostname, show_packets);
// }

// tcp_server_intf *get_mock_tcp_server(string hostname) {
//     return new mock_tcp_server(factory, hostname);
// }

// // Sets up server to receive connections on the given port
// void mock_tcp_server::setup_server(int port) {

// }

// // Tears down the server
// void mock_tcp_server::tear_down_server() {

// }

// // Accepts a connection on the server fd
// // Returns the socket to the accepted connection
// int mock_tcp_server::accept_connection() {

// }

// void mock_tcp_server::close_connection(int client_socket) {

// }

// ssize_t mock_tcp_server::read_from_client(int client, char *buffer, size_t count) {

// }

// ssize_t mock_tcp_server::write_to_client(int client, const char *buffer, size_t count) {

// }

// int mock_tcp_client::setup_connection(std::string host, std::string port) {

// }

// ssize_t mock_tcp_client::read_from_server(int socket, char *buffer, size_t count) {

// }

// ssize_t mock_tcp_client::write_to_server(int socket, const char *buffer, size_t count) {

// }

// void mock_tcp_client::close_connection(int socket) {

// }
